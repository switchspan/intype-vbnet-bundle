{
	content: 'private ${1:var_name} as ${2:data_type}()
	
default public readonly property item(byval nIndex as integer) as ${2:data_type}
	get
		return ${1:var_name}(nIndex)
	end get
end property'
	title: 'indexer'
	scope: 'source.vbnet'
	tab_trigger: 'indexer'
}