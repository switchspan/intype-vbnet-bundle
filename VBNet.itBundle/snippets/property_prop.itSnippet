{
	content: 'private ${1:var_name} as ${2:data_type}
	
public property ${3:prop_name}() as ${2:data_type}
	get
		return ${1:var_name}
	end get
	set (byval value as ${2:data_type})
		${1:var_name} = value
	end set
end property'
	title: 'property'
	scope: 'source.vbnet'
	tab_trigger: 'prop'
}